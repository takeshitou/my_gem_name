require 'spec_helper'

describe MyGemName do
  it 'has a version number' do
    expect(MyGemName::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
